name: form_factory
description: "A new Flutter package project."
version: 0.0.1

publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cl_basic_types:
    path: ../cl_basic_types
  flutter:
    sdk: flutter
  colan_widgets:
    path: ../colan_widgets

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
