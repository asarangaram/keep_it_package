name: form_factory
description: "A new Flutter package project."
version: 0.0.1

publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  colan_widgets:
    path: ../colan_widgets
  keep_it_state: 
    path: ../keep_it_state

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
